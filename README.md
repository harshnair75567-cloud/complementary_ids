# complementary_ids
a python code which works as a complement to a traditional ids system . This can see through incoming traffic sent a message of connection accepted to the attacker and move the attackers ip address to a text file then cut off the connection the file name and port can be customised inside the code
